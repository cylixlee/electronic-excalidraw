{
  "name": "excalidraw-app",
  "version": "1.1.0",
  "description": "Excalidraw Desktop Application",
  "author": "Excalidraw Team",
  "main": "dist/electron/main.js",
  "private": true,
  "homepage": ".",
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not ie <= 11",
      "not op_mini all",
      "not safari < 12",
      "not kaios <= 2.5",
      "not edge < 79",
      "not chrome < 70",
      "not and_uc < 13",
      "not samsung < 10"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "callsites": "4.2.0",
    "i18next-browser-languagedetector": "6.1.4",
    "idb-keyval": "6.0.3",
    "jotai": "2.11.0",
    "react": "19.0.0",
    "react-dom": "19.0.0",
    "uqr": "0.1.2",
    "vite-plugin-html": "3.2.2"
  },
  "prettier": "@excalidraw/prettier-config",
  "scripts": {
    "build:electron:app": "cross-env ELECTRON=true vite build",
    "build:electron:main": "tsc -p electron/tsconfig.json",
    "build:electron": "yarn build:electron:app && yarn build:electron:main",
    "electron:dev": "concurrently \"cross-env ELECTRON=true vite\" \"wait-on http://localhost:3000 && electron .\"",
    "electron:build": "yarn build:electron && electron-builder",
    "electron": "electron ."
  },
  "devDependencies": {
    "concurrently": "9.2.1",
    "electron": "41.1.0",
    "electron-builder": "26.8.1",
    "wait-on": "9.0.4"
  },
  "build": {
    "appId": "com.excalidraw.desktop",
    "productName": "Excalidraw",
    "directories": {
      "output": "release"
    },
    "files": [
      "build/**/*",
      "dist/electron/**/*"
    ],
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        }
      ],
      "icon": "../public/favicon.ico"
    },
    "mac": {
      "target": [
        "dmg"
      ],
      "icon": "public/icons/icon.icns"
    },
    "linux": {
      "target": [
        "AppImage"
      ],
      "icon": "public/icons"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true
    }
  }
}